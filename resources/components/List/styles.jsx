import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  userItem: {
    flexDirection: 'row',
    gap: 20,
    alignItems: 'center',
  },
  itemContainer: {
    marginVertical: 8,
  },
});

export default styles;
